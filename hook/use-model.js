import { useState } from "react";
import { OverlayBG } from "../components/container";
import { CSSTransition } from "react-transition-group";
const useModel = (defaultCondition) => {
    const [isOpen, setIsOpen] = useState(false);

    
}